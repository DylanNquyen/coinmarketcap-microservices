# Required only for the AI Copilot feature. Never commit a real key.
GEMINI_API_KEY=replace_with_your_gemini_api_key

# Optional. Defaults to 180000 milliseconds (3 minutes).
COIN_REFRESH_INTERVAL_MS=180000
